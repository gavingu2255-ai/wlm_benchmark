# .env.example  —  WLM Benchmark API Keys
# Copy this file to .env and fill in your keys.
# Never commit .env to version control.

# Anthropic API key (required for Claude agent + Claude judge)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI API key (required for GPT-4o / GPT-4o-mini agent + GPT-4o judge)
OPENAI_API_KEY=sk-your-key-here

# Ollama runs locally — no key needed.
# Make sure Ollama is running before starting the experiment:
#   ollama pull qwen2.5:7b
#   ollama pull gemma3:4b
